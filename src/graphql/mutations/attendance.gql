mutation RecordCheckIn($tournamentId: ID!) {
  recordCheckIn(tournamentId: $tournamentId) {
    streak {
      currentStreak
      longestStreak
      freezesAvailable
      lastCheckInAt
    }
    alreadyCheckedIn
    freezeUsed
    isComeback
    isNewLongest
  }
}
