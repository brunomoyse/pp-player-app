// Expo's generated `expo-env.d.ts` (gitignored) references these same Expo
// globals — including the `declare module '*.css'` that the `@/global.css`
// side-effect import needs. Reference them from this tracked file too so
// `tsc` type-checks identically in CI, where the generated file is absent.
/// <reference types="expo/types" />
/// <reference types="nativewind/types" />
